import { PictureContainer } from "./pictureContainer"

export const ContentContainer = () => {
  return (
    <section className='w-full h-auto border-solid border-2 border-sky-500'>
        <PictureContainer/>
        <PictureContainer/>
        <PictureContainer/>
    </section>
  )
}
